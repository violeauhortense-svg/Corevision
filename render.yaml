services:
  - type: web
    name: corevision-api
    env: docker
    plan: standard
    region: frankfurt
    numInstances: 1

    # Envs - À remplir depuis Render dashboard
    envVars:
      - key: PORT
        value: "3000"
      - key: DATA_DIR
        value: "/data"
      - key: UPLOADS_DIR
        value: "/uploads"
      - key: JWT_SECRET
        value: "change-me-in-production-min-32-chars!!"
      - key: API_BASE_URL
        value: "https://corevision-api.onrender.com"
      - key: NODE_ENV
        value: "production"
      # Supabase Configuration (REQUIRED for Hub Communication)
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false

    # Health check
    healthCheckPath: /make-server-cac859af/health

    # Auto-deploy on push
    autoDeploy: true
